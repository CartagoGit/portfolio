/**
 * Single import surface for every BEM block name used across the portfolio.
 * Components and templates should pull names from here instead of duplicating
 * raw `'portfolio__hero'` strings everywhere.
 */
export * from './portfolio.bem';
